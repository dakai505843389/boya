<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hd.clc.boya.db.impl.UserSelectionClassMapMapper">

    <sql id="Base_Column">
        id, userId, classId, isGroup, isPaid, paymentId, status, isEvalueted, addTime, updateTime
    </sql>

    <select id="queryById" resultType="com.hd.clc.boya.db.entity.UserSelectionClassMap">
        SELECT <include refid="Base_Column" />
        FROM User_Selection_Class_Map
        WHERE id = #{id}
        AND isDeleted = 0
    </select>

    <update id="deleteById">
        UPDATE User_Selection_Class_Map SET isDeleted = 1
        WHERE id = #{id}
    </update>

</mapper>