<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hd.clc.boya.db.impl.ClassMapper">

    <sql id="Base_Column">
        id, belongTeacherId, className, description, classImagePath, numberLimit, countNumber ,singlePrice,
        isAllowGroup, groupPrice, groupNumberLimit, classTypeId, classRoomId, classBeginTime, classEndTime,
        classAddTime, classUpdateTime, status, isDeleted
    </sql>

    <select id="queryById" resultType="com.hd.clc.boya.db.entity.Class">
        SELECT <include refid="Base_Column" />
        FROM Teacher
        WHERE id = #{id}
        AND isDeleted = 0
    </select>

    <update id="deleteById">
        UPDATE TABLE SET isDeleted = 1
        WHERE id = id
    </update>

</mapper>